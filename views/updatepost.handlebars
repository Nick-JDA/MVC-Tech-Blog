{{!-- <div class="text-center">
  <h2>{{title}}</h2>
  <p>{{content}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p> --}}

  <div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Update Post:</h3>

    <form class="form new-project-form">
      <div class="form-group">
        <label for="project-name">post name:</label>
        <input class="form-input" type="text" id="project-name" name="project-name" value="{{title}}" />
      </div>
      <div class="form-group">
        <label for="project-desc">content:</label>
        <textarea class="form-input" id="project-desc" name="project-desc">{{content}}</textarea>
      </div>
      <div class="form-group">
        <button id="update" type="click" class="btn btn-primary" data-id="{{id}}">Update</button>
      </div>
    </form>
  </div>

  <script src="/js/updatePost.js"></script>